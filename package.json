{
  "name": "orbit-explorer-web",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Sistema web de evaluación y exploración de cuerpos celestes.",
  "scripts": {
    "build": "node build.mjs",
    "start": "node server.js",
    "check": "node build.mjs && node --check app.bundle.js && node --check app.js && node --check evaluation.js && node --check data.js && node --check charts.js && node --check server.js",
    "test": "node --test tests/*.test.mjs"
  }
}
